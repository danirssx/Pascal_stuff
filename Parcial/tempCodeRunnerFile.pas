 If (n = n2) Then
      //     Begin
      //       palabra := copy(frase, contador, n);

      //       resultado := InvertidaPalabra(n, palabra);

      //       Delete(frase, contador, n);
      //       Insert(resultado, frase, contador);

      //       n2 := 0;
      //     End;